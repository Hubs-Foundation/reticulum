#!/bin/sh

export RELEASE_COOKIE=$NODE_COOKIE
export RELEASE_DISTRIBUTION=name
export RELEASE_NODE=ret@$NODE_NAME

case $RELEASE_COMMAND in
  start*|daemon*)
    ELIXIR_ERL_OPTIONS="-kernel inet_dist_listen_min 9000 inet_dist_listen_max 9100"
    export ELIXIR_ERL_OPTIONS
    ;;
  *)
    ;;
esac
